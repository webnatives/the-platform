app.directive('teamItem', () => ({
    teamUrl: 'team.html',
    controllerAs: 'team',
    bindToController: true,
    scope: {},
    controller($timeout) {

        var init = () => {
        };

        init();

        _.extend(this, {
        });
    }
}));
