'use strict';

app.directive('articleShareItem', (API, State, Helper) => ({
    templateUrl: 'article-share.html',
    scope: {
    },
    link(scope, element, attrs) {

        var init = () => {

        };

        init();

        scope = _.extend(scope, {

        });
    }
}));
